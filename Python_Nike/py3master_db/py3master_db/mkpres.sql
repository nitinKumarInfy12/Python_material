CREATE TABLE presidents (
    termnum int primary key,
    lastname varchar(32),
    firstname varchar(64),
    termstart varchar(10),
    termend varchar(10),
    birthplace varchar(128),
    birthstate varchar(32),
    birthdate varchar(10),
    deathdate varchar(10),
    party varchar(32)
)
